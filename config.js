// onda config — fill these in after creating your Supabase project
// (Project Settings -> API). The anon/public key is safe to commit;
// it's the client key meant to be public, access is controlled by RLS.
window.ONDA_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY"
};
